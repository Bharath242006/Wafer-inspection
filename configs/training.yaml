# Training Configuration for DriftSense-X

epochs: 20
batch_size: 16
learning_rate: 0.001
weight_decay: 0.0001
seed: 42

optimizer: "adamw"
scheduler: "cosine"
warmup_epochs: 2

checkpoints_dir: "weights/checkpoints"
trained_models_dir: "weights/trained"
logs_dir: "outputs/logs"
